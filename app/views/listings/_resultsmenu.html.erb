    <% new_sort_by = (@sort_by == :asc) ? :desc : :asc

            #the long version of the above
            # new_sort_by = if sort_by == :asc
            #     :desc
            # else
            #     :asc
            # end

    %>


<nav class="navbar navbar-default" >
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#filter-navbar-collapse-1">
        <span class="sr-only">Toggle filters and sorting options</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="filter-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown">Sort Results<b class="caret"></b></a>
          <ul class="dropdown-menu">
            <li><%= link_to "Sort by Name #{new_sort_by}", listings_path(:sort => :name, :sort_by => new_sort_by) %></li>
            <li class="divider"></li>
            <li><%= link_to "Sort by Location #{new_sort_by}", listings_path(:sort => :location, :sort_by => new_sort_by) %></li>
            <li class="divider"></li>
            <li>PUT THAT LINKTO HERE</li>
            <li class="divider"></li>

          </ul>
        </li>
      </ul>

      <ul class="nav navbar-nav">
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown">Filter Results<b class="caret"></b></a>
          <ul class="dropdown-menu">
            <li><%= link_to "Filter by Grabs", listings_path(:filter => 'grab') %> </li>
            <li class="divider"></li>
            <li><%= link_to "Filter by Swaps", listings_path(:filter => 'swap') %></li>
            <li class="divider"></li>
            <li><%= link_to "Filter by things to buy", listings_path(:filter => 'buy') %></li>
            <li class="divider"></li>
            <li><%= link_to "Filter by Qwerties", listings_path(:filter => 'fdsjfdsjk') %></li>
            <li class="divider"></li>
          </ul>
        </li>
      </ul>

 
      
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>